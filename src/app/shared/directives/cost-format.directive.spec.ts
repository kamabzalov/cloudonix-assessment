import { CostFormatDirective } from './cost-format.directive';

describe('CostFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CostFormatDirective();
    expect(directive).toBeTruthy();
  });
});
